server:
  port: 9082

spring:
  application:
    name: order-service
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config-server
      fail-fast: true
      enabled: true

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/
